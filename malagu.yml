# 依赖的公共前端 UI 框从 CDN 上加载
# mode: cdn
mode:
  - locale
  - theme
  - widget
  - shell
deployConfig:
  service:
    name: cellbang
    logConfig: 
      logstore: logstore
      project: cellbang
    role: acs:ram::${deployConfig.profile.accountId}:role/cellbangrole
malagu:
  name: 细胞矩阵 | Cellbang